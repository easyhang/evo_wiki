# Third-party notices

## LightRAG

The demo starts LightRAG from the pinned container image:

`{{LIGHTRAG_IMAGE}}`

The image itself is downloaded from GHCR on first use and is not embedded in
this ZIP. The LightRAG license text is included at
`licenses/LightRAG-LICENSE`.

## Python dependencies

The first run installs the versions listed in `requirements.txt` from the
recipient's configured Python package index. Those packages are not embedded
in this ZIP and remain subject to their respective licenses.
